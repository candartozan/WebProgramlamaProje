@model List<WebProgramlamaProje.Entities.Product>

@if (!string.IsNullOrEmpty(Convert.ToString(Session["UserName"])))
{
    <h4 class="text-center">Mağazamıza hoşgeldin @Session["UserName"]</h4>
}

<div class="display-6 my-3 text-center">En Son Eklene Ürünler</div>
<div class=" d-flex justify-content-center">
    <div class="row my-3">
        <div class="col-md-12">
            <div class="row">
                @{ Html.RenderPartial("_ShowcasePartial", Model); }
            </div>
        </div>
    </div>
</div>